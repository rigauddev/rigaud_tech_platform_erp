# Sales

Módulo reservado para contexto de vendas.

Estrutura independente preparada com camadas `application`, `domain`, `infrastructure`, `presentation` e `tests`.

Nenhuma regra de negócio foi implementada nesta etapa.
