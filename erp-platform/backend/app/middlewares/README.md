# Middlewares

Camada de middlewares HTTP compartilhados do backend.

Inclui preparação para request id, logging de requisições, CORS e compression.

Nenhuma regra de negócio foi implementada nesta etapa.
