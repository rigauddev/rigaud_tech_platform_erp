# Requirements

Dependências Python separadas por perfil de instalação.

O `pyproject.toml` continua sendo a fonte principal do pacote. Estes arquivos existem para apoiar ferramentas que esperam arquivos `requirements/*.txt`.
