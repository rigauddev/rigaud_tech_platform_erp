# App Config

Configuração por ambiente para o frontend Flutter.

Variáveis preparadas:

- `APP_NAME`
- `APP_ENV`
- `API_BASE_URL`
- `LOG_LEVEL`

Use `--dart-define` para informar valores em build ou execução.

Nenhum segredo deve ser armazenado no código.
