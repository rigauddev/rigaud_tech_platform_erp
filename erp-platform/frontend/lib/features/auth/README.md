# Auth

Feature de autenticação e identidade.

Estrutura em `data`, `domain` e `presentation`.

Inclui tela de login, controller Riverpod, repository, datasource remoto, storage seguro e integração com tokens.

Na DEV-010, a feature também inclui modelos e contratos para contexto ativo multi-empresa e multi-filial.
