# Dashboard

Feature reservada para visão inicial e indicadores futuros do ERP.

Estrutura preparada em `data`, `domain` e `presentation`.

Nesta task existe apenas um placeholder visual. Nenhuma regra de negócio foi implementada.
