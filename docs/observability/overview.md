# Observability Overview

A DEV-008 introduz a base de observabilidade.

Fluxo:

```text
request_id -> log estruturado -> mensagem -> resposta -> auditoria
```

Logs técnicos são para diagnóstico. Auditoria é registro imutável de ação relevante.
